import type { GameState } from '../../../shared/types.js'
import type { Match } from './Match.js'

export function updatePhysics(match: Match, _state: GameState): void {
  // Stub — will be implemented in task 02-ball-physics
}
