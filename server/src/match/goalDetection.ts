import type { GameState } from '../../../shared/types.js'
import type { Match } from './Match.js'

export function checkGoal(_match: Match, _state: GameState): void {
  // Stub — will be implemented in task 01-goal-detection (phase 3)
}
